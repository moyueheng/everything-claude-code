{
  "_comment": "这是 hooks 配置模板，需要手动合并到 ~/.claude/settings.json 中",
  "_instructions": "将 hooks 字段的内容合并到 settings.json 的 hooks 对象中",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ExitPlanMode",
        "description": "当用户接受计划后自动注入 TDD 工作流上下文",
        "hooks": [
          {
            "type": "command",
            "command": "uv run --project $HOME/.claude/hooks $HOME/.claude/hooks/on-exit-plan-mode.py"
          }
        ]
      }
    ]
  }
}
